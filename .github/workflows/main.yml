name: main
on: [push, pull_request]
jobs:
  Start_Server:
    runs-on: windows-latest
    steps:
      - run: git clone https://github.com/andriy332/KinashServer.git
      - run: cd KinashServer & dir & node app.js
