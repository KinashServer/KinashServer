name: main
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: mkdir i & cd i & git clone https://github.com/andriy332/KinashServer.git
      - run: npx standard
